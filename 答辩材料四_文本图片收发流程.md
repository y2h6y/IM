# 答辩材料四：文本与图片收发流程

> **核心思路**：消息收发看起来简单，但我们发现背后有很多容易被忽略的问题——消息会丢吗？会重复吗？图片会被服务端看到吗？用户等图片的体验怎么优化？我们从每一个可能出问题的环节出发，逐步完善整个收发链路。

---

## 一、文本消息：我们想清楚了哪些问题

```
我们思考的问题清单
──────────────────────────────────────────────────────────────
① 消息发出去了，对方没在线，消息会丢吗？
② 网络抖动重发，对方会收到两条一样的消息吗？
③ 进聊天时会话 ID 还没有，消息能发吗？
④ 消息是先乐观更新本地，还是等服务端确认？
⑤ 两个人的消息时序如何保证一致？
```

### 1.1 消息会丢吗：服务端持久化 + 增量同步

IM 消息的可靠传递分两层：

**第一层——服务端持久化**：消息到达服务端后立即写入 PostgreSQL，即使接收方当时不在线，消息也不会丢失。

**第二层——增量拉取补全**：接收方登录后，用 `since_id` 从服务端增量拉取离线期间所有消息：

```
接收方重新上线
    │
    ▼
本地最大消息 ID = 1024
    │
    ▼
GET /api/messages?since_id=1024
    │
    ▼
服务端返回 1024 之后的所有消息（含离线期间所有消息）
    │
    ▼
解密 → 存本地 → 渲染
```

这样无论对方离线多久，消息一条不漏。

### 1.2 消息会重复吗：client_msg_id 唯一约束

**我们的担忧**：网络不稳定时，客户端可能超时后重试发送，服务端可能收到同一条消息两次。

**方案**：每条消息在客户端生成全局唯一的 `client_msg_id`（UUID）。服务端建部分唯一索引：

```sql
CREATE UNIQUE INDEX idx_msg_client_id
ON messages(client_msg_id)
WHERE client_msg_id IS NOT NULL;
```

无论客户端重试多少次，服务端用 `ON CONFLICT DO NOTHING` 保证只落一条。接收方本地同样以服务端 `id` 为主键去重（`INSERT OR REPLACE`），最终用户永远看不到重复消息。

### 1.3 乐观更新 vs 等服务端确认

**我们的取舍**：
- 等服务端确认再显示：发送时间长，用户感觉卡顿
- 乐观更新（先本地追加显示）：用户体验流畅，像微信一样即时

**我们的选择**：乐观更新。消息发出后立即追加到本地消息列表，WebSocket `ack` 回来后用服务端真实 `conversationId` 更新本地记录。

---

## 二、会话建立：进聊天前必须先有 conv_id

**我们发现的问题**：从联系人列表点击某人进入聊天，此时服务端可能还没有这两人之间的会话记录，`conversationId` 为 0。如果直接发消息，服务端会因为找不到合法会话而返回 `no permission`。

**根本原因**：我们没有提前处理"首次建立会话"的场景。

**我们的思考**：会话应该在进入聊天页之前就确保存在，而不是发消息时才发现不存在。

**方案**：进入聊天前调用 `getOrCreateConversation` 接口，服务端如果不存在则自动创建，返回真实 `convId`：

```
点击联系人
    │
    ▼
POST /api/conversations { other_user_id: X }  ← 存在则返回已有，不存在则创建
    │
    ▼
拿到真实 conv_id
    │
    ▼
跳转 ChatView（conv_id > 0，发消息不会失败）
```

---

## 三、图片消息：我们考虑了哪些问题

```
图片收发的问题清单
──────────────────────────────────────────────────────────────
① 图片内容安全    → 服务端存储的图片能被管理员或黑客看到吗？
② 重复上传浪费    → 同一张截图发多次，每次都要上传吗？
③ 大图等待时间长  → 用户发完要等多久对方才能看到？
④ 加载时布局跳动  → 图片没加载完尺寸未知，气泡忽大忽小体验差
⑤ 缩略图过大     → 本来是为了秒显，结果比原图还大
```

---

## 四、图片安全：图片也要端到端加密

**我们的担忧**：文本消息做了 E2EE，但图片是直接存到 MinIO（对象存储），服务端能直接打开图片 URL 看到内容。这违背了"服务端零知识"的设计原则。

**方案**：图片 URL 本身也走 E2EE 加密载荷：

```
不加密的情况（我们排除了）：
上传图片 → 服务端存明文图片 → 发送 URL → 接收方直接访问
问题：服务端存了原始图片，管理员/黑客可见

我们的方案：
上传图片 → MinIO 存储（文件本身存在，但 URL 只有加密后才知道）
把 URL 打包成 E2EE 载荷：{ t:"image", v:url, thumb:thumbUrl, w:1920, h:1080 }
E2EE 加密整个载荷 → WebSocket 发送密文
接收方解密 → 得到 URL → 访问图片
```

服务端只知道"有人上传了一张图"，不知道这张图在哪个聊天里被发送、发给了谁。

---

## 五、重复上传：SHA256 内容寻址 + 秒传

**我们的担忧**：IM 里发截图是常见行为，同一张图（比如一张表情包）可能被几十个用户分别上传几十次，存储浪费且耗时。

**我们的思路**：如果两张图片内容完全相同，SHA256 哈希值一定相同。以哈希命名存储，天然去重：

```
用户选图
    │
    ▼
计算 SHA256 哈希（本地计算，不上传）
    │
    ▼
HEAD /api/check-file?hash={sha256}
    ├─ 200：服务端已有 ─────► 直接使用返回的 URL（零上传，秒完成）
    └─ 404：服务端没有 ─────► 上传原图，MinIO 以 hash.jpg 命名存储
```

对于经常传相同图片的场景，上传时间从几秒降为毫秒级。

---

## 六、加载体验：缩略图先显 + 气泡预占位

### 6.1 为什么需要缩略图

**我们的问题**：原图可能几 MB，在弱网环境下用户要等很久才能看到图片，这是不可接受的。

**方案**：上传时同时生成并上传缩略图（长边 ≤ 198px），接收方先展示缩略图（体积约 8KB，几乎秒显），点击后再懒加载原图：

```
接收到图片消息（含 thumbUrl + originalUrl）
    │
    ▼
AsyncImage(url: thumbUrl)  ← 先加载缩略图（8KB，100ms 以内）
    │  用户点击
    ▼
Sheet: AsyncImage(url: originalUrl)  ← 再加载原图
```

### 6.2 缩略图尺寸问题：Retina 的陷阱

**我们发现的问题**：缩略图的目标是 198 逻辑像素，但实际上传的文件是 594 物理像素（约 120KB），比预期大了 9 倍，秒显的意义打了折扣。

**根因分析**：`UIGraphicsImageRenderer` 默认采用屏幕的 `nativeScale`（iPhone 上是 3.0），逻辑分辨率乘以 3 倍才是实际渲染分辨率。

**我们的理解**：Retina 屏幕的"高清"是 UI 领域的概念，而我们要的缩略图应该是物理像素的 198px，不需要 Retina 放大。

**修复**：
```swift
let format = UIGraphicsImageRendererFormat()
format.scale = 1.0  // 明确告诉渲染器：1 逻辑像素 = 1 物理像素
let renderer = UIGraphicsImageRenderer(size: CGSize(width: 198, height: 198),
                                        format: format)
```

修复后缩略图从 120KB 降至 8KB，真正实现秒显。

### 6.3 气泡布局跳动：没有尺寸信息时的处理

**我们的担忧**：图片气泡在图片未加载前尺寸未知，加载完成后突然撑开，消息列表发生位移和跳动，体验很差。

**方案**：将图片的原始宽高随消息一起存入本地数据库，气泡在图片加载前就能按比例预占位置：

```swift
// 气泡渲染时：不管图片有没有加载，先按存储的宽高比计算占位尺寸
private var bubbleSize: (CGFloat, CGFloat) {
    guard message.imageWidth > 0, message.imageHeight > 0
        else { return (200, 150) }  // 没有尺寸信息给默认值
    // 按比例裁到最大 200×280，宽高比不变
    let ratio = CGFloat(message.imageWidth) / CGFloat(message.imageHeight)
    ...
}
```

图片还在加载时，灰色占位区域的尺寸已经正确，加载完成后内容填充进去，布局不发生任何跳动。

---

## 七、完整收发链路总览

```
发送方                        服务端                          接收方
──────                        ──────                          ──────
【文本消息】
输入文字
E2EE 加密
生成 client_msg_id
WS 发送密文 ────────────► 落库（唯一索引去重）
                          Redis PUBLISH ─────────────────► WS 推送密文
◄── ACK（conv_id）         记录 conv_id                      E2EE 解密
乐观更新：本地显示                                             saveMessage（本地存储）
                                                              FTS trigger（写索引）

【图片消息】
选图 → SHA256
HEAD 秒传检测 ───────────► 已有：返回 URL
                          没有：接收上传 → MinIO 存储
生成缩略图（198px, scale=1.0）
上传缩略图 ──────────────► MinIO 存储
E2EE 加密 {url, thumb, w, h}
WS 发送密文 ────────────► 落库转发 ──────────────────────► 解密得到 url/thumbUrl/尺寸
                                                              气泡预占位（按宽高比）
                                                              先显缩略图（8KB）
                                                              点击后懒加载原图
```

---

## 八、设计决策总览

```
我们担心的问题               解决方案                          效果
───────────────────────────────────────────────────────────────────────
消息丢失                    服务端持久化 + since_id 增量同步     离线多久消息都在
消息重复                    client_msg_id 唯一索引              重试 N 次 = 只有 1 条
首次进聊天失败              进入前 getOrCreateConversation       conv_id 永远 > 0
图片被服务端看到            图片 URL 纳入 E2EE 载荷              服务端不知图片发给谁
重复上传浪费                SHA256 内容寻址 + 秒传检测           相同图片零流量
等图片时间长                上传即生成缩略图，接收先显缩略图       感知延迟 < 100ms
缩略图比原图大              UIGraphicsImageRenderer scale=1.0   8KB vs 120KB
气泡布局跳动                宽高随消息存库，气泡预占位            无跳动，体验流畅
```
