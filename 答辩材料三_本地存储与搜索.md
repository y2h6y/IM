# 答辩材料三：本地消息存储与全文搜索

> **核心思路**：本地存储要解决两个方向的问题：一是**数据安全**（本地数据不该被别人读到）；二是**使用体验**（消息要快速可查、断网能用、搜索有结果）。我们从这两个方向分别推演威胁和需求，再倒推出设计方案。

---

## 一、我们考虑了哪些问题

```
安全方向
────────────────────────────────────────────────────────
① 手机被人翻看   → 本地数据库文件直接可读，消息全部暴露
② 手机丢失被盗   → 不解锁也能通过工具导出沙盒文件
③ iCloud/iTunes 备份 → 备份文件里包含未加密的数据库

体验方向
────────────────────────────────────────────────────────
④ 断网时消息不可查  → 每次打开都要联网拉取，没网等于聋子
⑤ 多线程并发写入   → WebSocket 收消息同时后台同步，写入冲突崩溃
⑥ 历史消息重复     → 增量拉取时产生重复条目，列表显示双份
⑦ 大量消息搜索慢   → LIKE 扫全表，消息越多越卡
⑧ 中文搜索不准     → 普通分词无法精确匹配中文短语
```

---

## 二、安全方向：本地数据库怎么加密

### 2.1 我们首先想到的问题

手机应用的沙盒目录可以通过多种方式访问：
- 越狱设备：文件管理器直接浏览
- 连接 Mac：通过 `idevicebackup` 或 Xcode 导出沙盒
- 备份文件：iTunes 本地备份不加密时，数据库文件直接可读

如果数据库是普通 SQLite 文件，只要拿到文件就能用 DB Browser 打开，所有聊天记录一览无余。

### 2.2 我们考虑的加密方案对比

| 方案 | 加密层级 | 复杂度 | 我们的判断 |
|------|---------|-------|-----------|
| 不加密 | 无 | 零 | 不可接受 |
| SQLCipher | 应用层（密钥由 App 管理）| 高（需编译链集成）| 备用升级路径 |
| iOS Data Protection | 系统层（Secure Enclave 管理密钥）| 一行代码 | **选用** |
| 自己写加密逻辑 | 应用层 | 极高，且容易出错 | 绝对不做 |

**为什么选 iOS Data Protection 而不是 SQLCipher？**

我们研究后发现：在 iOS 上，Data Protection 的密钥由 Secure Enclave 硬件芯片管理，与用户锁屏密码绑定。这意味着：
- 不知道锁屏密码 = 拿不到密钥 = 文件无法解密
- 即使把手机存储芯片物理拆下来，加密也不会被破解（密钥在 Secure Enclave，不在存储芯片）

这实际上比纯软件的 SQLCipher 更难被绕过，而实现成本只有一行：

```swift
// 数据库文件 + WAL 辅助文件全部设置硬件加密
try FileManager.default.setAttributes(
    [.protectionKey: FileProtectionType.completeUnlessOpen],
    ofItemAtPath: dbPath
)
```

`completeUnlessOpen` 的含义：App 打开文件后即使锁屏也可继续读写（支持后台消息同步）；设备重启未解锁前文件完全不可访问。

---

## 三、体验方向：为什么需要本地存储

### 3.1 先问：能不能每次都从服务端拉？

**我们的否定理由**：
- 断网 = 打开 App 什么都看不到，体验极差
- 每次进聊天都要等网络请求，哪怕历史消息一条没变
- 大量历史消息重复传输，浪费流量和时间

**结论**：本地必须有完整的消息缓存，服务端只负责增量同步（`since_id` 机制）。

### 3.2 增量同步的思路

```
首次登录：
拉取所有会话 → 对每个会话拉取服务端最新消息 → 存入本地 SQLite

后续打开聊天：
先渲染本地消息（无需等待）
再拉取 since_id=本地最大消息ID 之后的增量 → 追加
→ 用户体验：秒开，静默更新

断网时：
完全依赖本地 SQLite，所有历史消息可查
```

---

## 四、并发安全：多个线程同时写数据库

**我们遇到的问题**：消息有两个写入来源：
1. WebSocket 实时收到消息 → `saveMessage()`
2. 登录后后台全量同步 → `backgroundSyncAll()` 批量写入

如果这两个操作同时触发，直接用原生 `SQLite3 C API` 会有并发写入冲突（SQLite 默认不支持多线程并发写）。

**我们的方案**：引入 GRDB（Swift Package），其核心是 `DatabaseQueue`——一个串行化的数据库操作队列：

```
Thread A（WebSocket 写）  ─┐
Thread B（backgroundSync 写）─┤─► DatabaseQueue（串行队列）─► SQLite
Thread C（UI 读）          ─┘         先到先得，互不冲突
```

所有读写操作排队执行，彻底消除并发冲突，同时 GRDB 的 WAL 模式允许读写并发，不影响 UI 流畅度。

---

## 五、消息重复：增量拉取的边界问题

**我们的担忧**：当 `since_id` 拉取遇到网络重试，或 WebSocket 实时消息恰好和增量拉取在同一时间点，同一条消息可能被写两次。

**方案**：`INSERT OR REPLACE` 以 `id`（服务端主键）为冲突检测依据：

```sql
-- 同一个 id 只保留最新一条，天然去重
INSERT OR REPLACE INTO local_messages (id, ...) VALUES (?, ...)
```

由于 `id` 是服务端分配的自增主键，同一条消息无论被写多少次，本地只保留一条。

---

## 六、全文搜索：为什么 LIKE 不够用

### 6.1 LIKE 的问题

```sql
-- LIKE 的本质是全表扫描
SELECT * FROM local_messages WHERE plain_content LIKE '%你好%'
-- 100 条消息：可接受
-- 10000 条消息：明显卡顿
-- 100000 条消息：严重卡顿，界面冻结
```

随着消息量增长，LIKE 查询时间线性增长，不可接受。

### 6.2 我们选择 FTS5 的理由

FTS5（Full-Text Search 5）是 SQLite 内置的倒排索引引擎：

```
建索引时：
"你好世界" → 分词 → {"你":行1, "好":行1, "世":行1, "界":行1}
"你是谁"   → 分词 → {"你":行2, "是":行2, "谁":行2}

查询"你好"时：
查倒排索引 "你" → {行1, 行2}
查倒排索引 "好" → {行1}
两者取交集 → 行1（"你好世界"）← 毫秒级完成，不扫全表
```

**为什么用 content table（外部内容表）而不是独立 FTS 表？**

如果创建独立 FTS 表，消息内容会被存两份（主表一份 + FTS 表一份），空间浪费且需要手动保持同步。

我们使用 `content=local_messages` 让 FTS5 的索引直接指向主表：

```sql
CREATE VIRTUAL TABLE messages_fts USING fts5(
    plain_content,
    content      = local_messages,  -- 不重复存数据，只存索引
    content_rowid = id
);
```

### 6.3 索引自动维护

我们不希望每次写消息后还要手动更新 FTS，所以用触发器自动维护：

```sql
-- 新消息写入时，自动更新 FTS 索引
CREATE TRIGGER messages_ai AFTER INSERT ON local_messages
WHEN new.msg_type IN ('text','e2e') AND new.plain_content != ''
BEGIN
    INSERT INTO messages_fts(rowid, plain_content)
    VALUES (new.id, new.plain_content);
END;
```

从此 `saveMessage()` 只需写主表，FTS 索引完全自动跟进。

### 6.4 初始化全量建索引的坑

**我们发现的问题**：历史消息（App 更新前已有的消息）不会触发 trigger，FTS 表是空的，搜索不到任何东西。

**我们的思考**：需要在首次安装新版本时，对所有历史消息做一次批量建索引。

**最初的错误方案**：
```swift
// 我们以为这样可以清空 FTS 再重建
try db.execute(sql: "DELETE FROM messages_fts")  // ← 对 content table 无效！直接报错被吞掉
try db.execute(sql: "INSERT INTO messages_fts SELECT ...")
```

**问题根因**：FTS5 的 content table 不支持直接 `DELETE`，这条语句会抛异常，但被外层 `catch` 静默吞掉，后续的 INSERT 也没有执行，`fts_populated` 标志永远没有被置为 `1`。

**正确方案**：FTS5 提供了专用的 rebuild 命令：
```swift
// 一条命令：让 FTS5 自己去读 local_messages，完整重建倒排索引
try db.execute(sql: "INSERT INTO messages_fts(messages_fts) VALUES('rebuild')")
```

---

## 七、搜索结果的呈现：定位到消息位置

光搜到还不够，我们还思考了：**搜到的消息怎么帮用户找到它在哪**？

方案：搜索结果携带 `conversationId` + `messageId`，点击后：
1. 跳转到对应 ChatView
2. `ScrollViewReader` 滚动到目标消息行
3. 橙色背景高亮 1.5 秒后淡出

FTS5 的 `snippet()` 函数自动在匹配词周围截取上下文并标注：
```sql
snippet(messages_fts, 0, '《', '》', '…', 20)
-- 输出示例："…昨天我们说到《你好》吗，对啊…"
```

---

## 八、整体设计总结

```
问题                         方案                           效果
──────────────────────────────────────────────────────────────────────
手机被盗/沙盒读取             iOS Data Protection AES-256    不知锁屏密码 = 文件不可读
断网消息消失                  本地 SQLite 全量缓存            断网可查所有历史消息
多线程写冲突                  GRDB DatabaseQueue 串行队列     并发安全，无崩溃
消息重复显示                  INSERT OR REPLACE 按 id 去重   无论重试多少次只保留一条
大量消息搜索卡顿              FTS5 倒排索引                  毫秒级查询，不随消息量退化
FTS 历史消息缺失              'rebuild' 命令初始化索引        所有历史消息均可搜索到
```
