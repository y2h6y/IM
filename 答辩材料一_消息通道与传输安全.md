# 答辩材料一：消息通道实现与传输安全

> **核心思路**：消息通道的安全不只是"加个 HTTPS"，我们思考的是：如果某个环节被攻破，消息内容的暴露范围有多大？能不能做到即使服务器被黑，历史消息也无法被读取？

---

## 一、我们考虑了哪些安全威胁

```
威胁面梳理
──────────────────────────────────────────────────────────────
① 服务端被入侵     → 服务器上存了明文消息，数据库全泄露
② 网络传输被截获   → HTTP 明文传输，中间人可读所有内容
③ 服务端管理员     → 运维人员能不能直接查库看用户聊天记录
④ 消息被重放攻击   → 截获密文后反复发送，让对方收到重复消息
⑤ 公钥被服务端篡改 → 服务端如果替换公钥，能否实施中间人攻击
⑥ 历史消息溯源     → 某次密钥泄露后，能否解密过去所有消息
```

---

## 二、最核心的问题：服务端能不能读消息

我们首先想清楚一件事：**服务端到底需不需要看到消息明文？**

答案是：不需要。服务端只需要做路由（把消息从 A 转发给 B），完全不需要知道内容。

这引出了**端到端加密（E2EE）**的设计目标：

```
不做 E2EE 的架构（我们首先排除的）：
Alice ──[明文]──► 服务端存储 ──[明文]──► Bob
问题：服务端被攻破 = 所有历史消息泄露

做 E2EE 的架构（我们的方案）：
Alice ──[密文]──► 服务端转发 ──[密文]──► Bob
                服务端只见密文，解不开
```

---

## 三、密钥从哪来：X25519 ECDH 密钥交换

**我们的问题**：Alice 和 Bob 从未见面，如何在服务端不知情的情况下协商出一个只有他们俩知道的密钥？

**方案**：Diffie-Hellman 密钥交换，使用 X25519 椭圆曲线：

```
Alice                         服务端（只存公钥）              Bob
──────                        ──────────────────              ────
生成密钥对：
  privateA（永不离开设备，存 Keychain）
  publicA  ──────────────────────────────────────────────────►（Bob 拉取时可得到）

                                                             生成密钥对：
                                                               privateB（存 Keychain）
                                                               publicB ────────────►（存服务端）

Alice 拉取 publicB
ECDH(privateA, publicB) = SharedSecret               ECDH(privateB, publicA) = 同一个 SharedSecret
                │                                                    │
         两端派生出完全相同的会话密钥，服务端无法计算（没有任何私钥）
```

**为什么选 X25519 而不是 RSA**：
- X25519 是目前公认最安全的椭圆曲线，TLS 1.3 首选
- 密钥短（32字节）、运算快（比 RSA-2048 快 20 倍）
- iOS CryptoKit 原生支持，无需第三方库

---

## 四、SharedSecret 够用吗：HKDF 密钥派生

**我们发现的问题**：ECDH 直接输出的 SharedSecret 在密码学上不够"随机均匀"，不适合直接作为 AES 密钥。

**方案**：用 HKDF-SHA256（基于哈希的密钥派生函数）对其进行派生：

```swift
// SharedSecret → 真正的 AES 密钥
let sessionKey = HKDF<SHA256>.deriveKey(
    inputKeyMaterial: sharedSecret,
    info: Data("QQQ-E2EE-v1".utf8),
    outputByteCount: 32  // AES-256 需要 32 字节
)
```

HKDF 保证：即使两对用户的 SharedSecret 相近，派生出的 AES 密钥也完全不同。

---

## 五、加密本身：为什么选 AES-256-GCM

**我们考虑的问题**：
- 只加密（不认证）：攻击者截获密文后可以**随机翻转比特位**，接收方解密后得到乱码但不知道被篡改了
- 需要**认证加密**：解密时如果内容被篡改，直接报错拒绝处理

**AES-256-GCM = 加密 + 认证 一体化**：
```
加密：AES-256（256位密钥，当前无法暴力破解）
认证：GCM模式附带 16字节 AuthTag
解密时：先验 AuthTag，AuthTag 错误 → 解密失败，拒绝展示
```

这保证了：密文被任何方式篡改，接收方立刻知道，消息不展示。

---

## 六、前向安全性：历史消息能被溯源吗

**我们的担忧**：如果某天 Alice 的手机被没收，私钥泄露，攻击者能解密过去所有和 Bob 的消息吗？

**理论上的问题**：X25519 是静态密钥（私钥一旦生成长期使用），如果私钥泄露，所有基于它派生的会话密钥都可被重新计算，历史消息全部暴露。

**我们的应对**：
- 当前方案：每次重新登录重新生成密钥对，新登录后老密文理论上用新密钥无法解密
- 已预留接口：`retryPeerKey()` 支持强制刷新密钥对，可扩展为"一次一密"的完整前向安全
- 私钥存 Keychain，`kSecAttrAccessibleAfterFirstUnlock` 级别，设备不解锁无法提取

---

## 七、传输层：WebSocket 的安全保障

**我们的考虑**：E2EE 保护了消息内容，但传输层还需要考虑：
- 连接被劫持：能否往连接里注入假消息？
- 身份伪造：连接建立时服务端如何确认"这真的是 Alice"？

**方案**：
```
1. WSS（WebSocket over TLS）：全程加密，防止网络层注入和窃听
2. 连接建立时携带 JWT Access Token：服务端校验 Token 合法性确认身份
3. 连接注册后绑定 userID：后续所有消息路由基于已验证的 userID，不信任消息头里的 from 字段
```

---

## 八、消息重复与重放问题

**我们的担忧**：网络抖动时客户端可能重发同一条消息；理论上攻击者可以截获密文后重复提交。

**方案**：每条消息携带全局唯一的 `client_msg_id`（UUID），服务端建部分唯一索引：

```sql
-- 同一个 client_msg_id 只能存一条消息
CREATE UNIQUE INDEX idx_msg_dedup
ON messages(client_msg_id)
WHERE client_msg_id IS NOT NULL;

-- 插入时用 ON CONFLICT DO NOTHING
INSERT INTO messages (...) VALUES (...) ON CONFLICT DO NOTHING;
```

无论网络重试多少次、截获多少次，服务端只接受第一条，后续全部静默丢弃。

---

## 九、整体安全架构总览

```
威胁                          防线                           效果
─────────────────────────────────────────────────────────────────────────
服务端被入侵/管理员查库         E2EE，服务端只存密文            数据库泄露 = 拿到一堆无法解密的密文
网络传输被截获                  WSS（TLS）+ E2EE 双重加密       即使 TLS 被破解，内容仍是密文
公钥被服务端篡改                当前依赖服务端公钥（已知限制）    预留 Trust-On-First-Use 升级路径
消息被篡改                      AES-256-GCM 认证加密            篡改后 AuthTag 校验失败，直接拒绝
消息重放攻击                    client_msg_id 唯一索引          重复提交 = 静默丢弃
私钥泄露后历史溯源               重登录换密钥对，私钥存 Keychain  旧密钥无法解新密文
```
