# 答辩材料二：登录协议安全设计

> **核心思路**：登录是整个系统的信任入口，我们从「哪些环节会被攻击、哪些数据会被泄露」出发，逐项设计防线，而不是上来就写代码。

---

## 一、我们考虑了哪些攻击和泄露场景

```
攻击面梳理
──────────────────────────────────────────────────────
① 数据库被脱库     → 密码哈希能被破解吗？
② Token 被网络截获  → 截到 Token 能冒充用户吗？
③ Token 类型被滥用  → Refresh Token 能当 Access Token 用吗？
④ 设备丢失/被盗     → 手机里的 Token 能被读出来吗？
⑤ 同账号多设备并发  → 多台设备同时在线会有幽灵连接吗？
⑥ 长期未登录的旧 Token → Token 永久有效会怎样？
```

---

## 二、场景一：数据库被脱库 → 密码如何防破解

**我们的担忧**：如果数据库被攻击者拿到，密码哈希能否被暴力破解或彩虹表查出来？

**分析**：
- 直接存明文密码：一旦泄露，所有用户密码直接暴露
- 只用 MD5/SHA1：彩虹表预计算攻击，常见密码秒查
- 只用 bcrypt：可以防彩虹表，但所有部署实例共用同一套哈希，攻击者拿到一份数据库就能复用攻击字典

**我们的方案：`bcrypt( SHA256(pepper ‖ password) )`**

```
用户输入密码 "123456"
        │
        ▼
SHA256("MY_RANDOM_PEPPER_STRING" + "123456")
= 一段 64 字节哈希（不同 pepper 结果完全不同）
        │
        ▼
bcrypt( 上述哈希, cost=12 )
= 最终存入数据库的字符串
```

- **pepper** 存在服务器环境变量（`.env`），不进数据库。即使数据库泄露，攻击者没有 pepper 就无法还原哈希。
- **bcrypt** 自带随机 salt 且计算极慢（cost=12 约 300ms/次），暴力破解成本高到不可接受。

```go
// 服务端验证密码
peppered := sha256.Sum256([]byte(pepper + inputPassword))
err := bcrypt.CompareHashAndPassword(storedHash, peppered[:])
```

---

## 三、场景二：Token 被截获 → 有效期与最小权限

**我们的担忧**：HTTPS 保护了传输，但如果客户端日志、代理、中间件意外记录了 Token，或者 Token 被 XSS / 其他途径窃取，攻击者能持续冒充用户吗？

**分析**：如果只用一个长期有效的 Token（如 30 天），被偷走意味着攻击者有 30 天的合法身份。

**我们的方案：双 Token，短命 Access + 长命 Refresh**

```
Access Token（2小时）   ← 用于所有业务接口，被偷最多用 2 小时
Refresh Token（30天）   ← 仅用于换新 Token，不带任何业务权限

正常使用：
每 2 小时 App 自动用 Refresh Token 静默换一对新 Token
（用户无感知）

Token 泄露场景：
Access 被偷  → 2 小时后自动失效
Refresh 被偷 → 服务端可以通过强制登出（踢 Token）撤销
```

---

## 四、场景三：Token 类型混淆 → 两套独立密钥

**我们的担忧**：最初一个 `JWT_SECRET` 签发所有 Token。我们意识到一个问题：

> Refresh Token 是 30 天有效的 JWT，如果服务端只校验签名和有效期，攻击者可以**直接把 Refresh Token 当 Access Token 提交**，绕过 2 小时限制。

**这是真实存在的 JWT 攻击向量**，称为"Token 类型混淆"（Type Confusion）。

**我们的修复**：

```
修复前：
JWT_SECRET 一个密钥 → 签发 Access、Refresh 两种 Token
服务端校验：只验签名 + 有效期 → Refresh 可以冒充 Access ⚠️

修复后：
ACCESS_SECRET  ← 只用于签发和校验 Access Token
REFRESH_SECRET ← 只用于签发和校验 Refresh Token

即使攻击者拿到 Refresh Token，
用 REFRESH_SECRET 签的 JWT 无法通过 ACCESS_SECRET 验签 → 直接拒绝
```

```go
// 校验 Access Token 时，只用 ACCESS_SECRET
token, _ := jwt.Parse(tokenStr, func(t *jwt.Token) (interface{}, error) {
    return []byte(os.Getenv("JWT_ACCESS_SECRET")), nil
})
// Refresh Token 是用 REFRESH_SECRET 签的，这里验签必然失败 → 无法冒充
```

---

## 五、场景四：手机丢失 → 本地 Token 不能被读取

**我们的担忧**：Token 存在哪里？手机被捡到，或者应用沙盒被恶意访问，Token 能被读出来吗？

**分析**：
- `UserDefaults`：明文 plist 文件，任何有沙盒访问权限的工具都能读
- 文件系统：同上
- `Keychain`：iOS 系统级加密存储，需要设备解锁 + 应用签名双重验证才能访问

**我们的方案**：所有敏感数据（Access Token、Refresh Token、E2EE 私钥）全部存 Keychain：

```swift
KeychainService.save(key: "qqq_auth_token",    value: accessToken)
KeychainService.save(key: "qqq_refresh_token", value: refreshToken)
KeychainService.save(key: "qqq_e2e_private_key", value: privateKeyBase64)

// 保护级别：kSecAttrAccessibleAfterFirstUnlock
// 含义：设备首次解锁后可访问（支持后台同步）
//       设备重启未解锁时不可访问（防物理拆机读取）
```

---

## 六、场景五：多设备同时在线 → 幽灵连接与消息泄露

**我们的担忧**：同一个账号在两台手机上都登录了，会不会有"幽灵设备"一直保持 WebSocket 连接，默默接收所有消息？

**分析**：这种场景下，旧设备的 WebSocket 连接没有主动断开，只要网络不断，它就会一直收到消息推送。这不仅是安全问题，也是资源浪费。

**我们的方案**：Gateway 在新设备连接时，强制断开同账号的旧连接：

```go
// 新 WebSocket 连接注册
func (h *Hub) register(newClient *Client) {
    if old, exists := h.clients[newClient.userID]; exists {
        // 通知旧设备"你已在其他设备登录"
        old.send <- KickMessage{reason: "另一台设备登录，当前会话已断开"}
        old.conn.Close()
    }
    h.clients[newClient.userID] = newClient
}
```

客户端收到踢出消息后，清空 Keychain 并跳回登录页，提示"你的账号已在其他设备登录"。

---

## 七、整体防护体系总览

```
攻击场景                    我们的防线                        关键实现
─────────────────────────────────────────────────────────────────────────
数据库脱库                  bcrypt + pepper 双重保护            cost=12 bcrypt，pepper 不入库
彩虹表/字典攻击              每用户独立 bcrypt salt              bcrypt 自带 salt
Token 长期有效               Access Token 2h 短命                双 Token 设计
Token 类型混淆               两套独立 Secret 签名                ACCESS/REFRESH_SECRET 分离
Token 被盗（短期）           2h 内自动失效                       Access 有效期控制
Token 被盗（长期）           Refresh Token 可强制撤销            服务端 Token 黑名单（预留）
本地 Token 泄露              iOS Keychain 系统级加密             kSecAttrAccessibleAfterFirstUnlock
设备共享/多端登录            同账号 WebSocket 互踢               Gateway 强制断旧连接
网络传输截获                 HTTPS + WSS 全程 TLS                URLSession 强制 TLS
```
