# QQQ 即时通讯 App —— 前后端架构设计答辩文档

> **项目定位**：面向 iOS 平台的 C2C 即时通讯应用，支持文字/图片消息、端到端加密（E2EE）、离线消息同步与全文历史检索。

---

## 一、整体架构全景图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          iOS Client (Swift / SwiftUI)                   │
│                                                                         │
│  ┌──────────┐  ┌──────────────┐  ┌───────────────┐  ┌───────────────┐ │
│  │ SwiftUI  │  │  AppState    │  │ ChatViewModel │  │SearchViewModel│ │
│  │  Views   │◄─┤ (全局状态机)  │  │  (会话状态)   │  │  (FTS 搜索)   │ │
│  └────┬─────┘  └──────┬───────┘  └──────┬────────┘  └───────────────┘ │
│       │               │                  │                              │
│  ┌────▼───────────────▼──────────────────▼────────────────────────┐    │
│  │                       Services Layer                           │    │
│  │  APIService   WebSocketService   E2EEService   DatabaseService │    │
│  │  (HTTP REST)  (WS 实时消息)      (X25519+AES)  (GRDB+FTS5)    │    │
│  └────┬──────────────┬──────────────────┬────────────────┬────────┘    │
│       │              │                  │                │             │
│  KeychainService  URLSession       CryptoKit        iOS Data           │
│  (JWT/E2E密钥)    (网络层)          (Apple原生)      Protection         │
└───────┼──────────────┼──────────────────────────────────────────────────┘
        │ HTTPS/REST   │ WSS
        ▼              ▼
┌───────────────────────────────────────────────────────────────────────┐
│                         Backend (Go)                                  │
│                                                                       │
│   ┌─────────────────────────┐    ┌──────────────────────────────┐    │
│   │    App Server (:8080)   │    │   Gateway Server (:8081)     │    │
│   │                         │    │                              │    │
│   │  /api/login             │    │  WebSocket Hub               │    │
│   │  /api/refresh           │    │  - 连接管理 & 互踢            │    │
│   │  /api/messages          │    │  - 消息路由 & 广播            │    │
│   │  /api/conversations     │    │  - Redis Pub/Sub 消费         │    │
│   │  /api/upload            │    │                              │    │
│   │  /api/users/me/pubkey   │    │                              │    │
│   └────────────┬────────────┘    └──────────────┬───────────────┘    │
│                │                                 │                    │
│   ┌────────────▼─────────────────────────────────▼───────────────┐   │
│   │                   Infrastructure Layer                        │   │
│   │                                                               │   │
│   │  ┌──────────────┐  ┌─────────────────┐  ┌────────────────┐  │   │
│   │  │  PostgreSQL  │  │      Redis      │  │     MinIO      │  │   │
│   │  │  持久化存储   │  │  Pub/Sub+Session │  │  对象存储(图片) │  │   │
│   │  │  用户/消息/   │  │  互踢/在线状态   │  │  SHA256内容寻址│  │   │
│   │  │  会话/公钥    │  │                 │  │  缩略图生成    │  │   │
│   │  └──────────────┘  └─────────────────┘  └────────────────┘  │   │
│   └───────────────────────────────────────────────────────────────┘   │
│                    Docker Compose 统一编排                             │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 二、技术选型对比

### 2.1 iOS 客户端框架

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **SwiftUI + MVVM** ✅ | 声明式 UI、响应式绑定、原生性能、Apple 全力维护 | 需要 iOS 14+ | **选用** |
| UIKit + MVC | 成熟稳定、控制精细 | 代码冗余、状态管理混乱 | 放弃 |
| React Native | 跨平台、JS 生态 | 桥接性能损耗、非原生体验 | 放弃 |
| Flutter | 跨平台、UI 一致 | Dart 生态、非 iOS 原生 API | 放弃 |

**选用理由**：项目聚焦 iOS 单平台，SwiftUI 的 `@Published` + `ObservableObject` 天然契合 IM 消息流的响应式更新；`async/await` 简化了 WebSocket 并发处理；Apple CryptoKit 提供原生 E2EE 加密支持。

---

### 2.2 后端语言与框架

| 方案 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **Go（原生 net/http）** ✅ | 高并发、低内存、编译型、goroutine 天然适配 WebSocket | 泛型较新 | **选用** |
| Node.js (Express) | JS 生态成熟、WebSocket 库丰富 | 单线程、CPU 密集型弱 | 放弃 |
| Java (Spring Boot) | 企业级稳定 | 启动慢、资源消耗大 | 放弃 |
| Python (FastAPI) | 开发快 | GIL 限制并发性能 | 放弃 |

**选用理由**：Go 的 goroutine-per-connection 模型对 WebSocket 长连接管理极为高效；标准库即可实现 HTTP + WebSocket，无需重型框架；单二进制部署方便 Docker 化。

---

### 2.3 进程架构：双进程分离

| 方案 | 适用场景 | 本项目选型 |
|------|---------|-----------|
| **App Server + Gateway 分离** ✅ | HTTP REST 与 WebSocket 职责解耦，独立扩展 | **选用** |
| 单体服务 | 小型项目快速迭代 | 不选（扩展性差） |
| 微服务（多服务） | 大型生产系统 | 不选（过度设计） |

```
App Server ──► 处理所有 REST API、鉴权、消息落库
Gateway    ──► 管理 WebSocket 长连接、消息实时推送
两者通过 Redis Pub/Sub 解耦通信
```

---

### 2.4 实时通信方案

| 方案 | 延迟 | 实现复杂度 | 结论 |
|------|------|-----------|------|
| **WebSocket** ✅ | 极低（全双工） | 中 | **选用** |
| HTTP 短轮询 | 高（间隔延迟） | 低 | 放弃 |
| HTTP 长轮询 | 中 | 中 | 放弃 |
| SSE（Server-Sent Events）| 低（单向） | 低 | 不满足双向需求 |
| MQTT | 低（IoT 优化）| 需额外 Broker | 过重 |

**选用理由**：IM 场景要求客户端既能主动发送、也能被动接收，WebSocket 全双工特性最匹配；iOS 原生 `URLSessionWebSocketTask` 无需第三方库。

---

### 2.5 数据库选型

| 数据库 | 用途 | 选型理由 |
|--------|------|---------|
| **PostgreSQL** ✅ | 用户、消息、会话、公钥持久化 | ACID 事务、JSON 支持、部分唯一索引（消息去重）|
| **Redis** ✅ | 在线状态、Session、Pub/Sub | 内存级速度、原生 Pub/Sub 解耦双进程通信 |
| **MinIO** ✅ | 图片对象存储 | S3 兼容、本地部署、SHA256 内容寻址秒传 |
| **SQLite（本地）** ✅ | 本地消息缓存 + FTS 搜索 | 嵌入式、零配置、FTS5 支持全文检索 |

---

### 2.6 本地数据库安全

| 方案 | 加密方式 | 实现成本 | 结论 |
|------|---------|---------|------|
| **iOS Data Protection** ✅ | AES-256，Secure Enclave 密钥 | 一行代码 | **选用** |
| SQLCipher | AES-256，应用层密钥 | 需第三方编译链 | 备选（升级路径） |
| 不加密 | 无 | 零成本 | 不可接受 |

> SQLCipher 提供应用层加密（防已 root 设备读取），iOS Data Protection 提供系统层加密（防物理盗取）。当前 iOS 26.5 目标平台 Secure Enclave 强制开启，两者防护等级相当；已预留升级至 SQLCipher 的接口设计。

---

### 2.7 安全体系选型

| 模块 | 方案 | 对比放弃方案 |
|------|------|-------------|
| **密钥交换** | X25519 ECDH | RSA（性能差）、ECDH P-256（稍弱）|
| **消息加密** | AES-256-GCM | AES-CBC（无认证）、ChaCha20（iOS 次优）|
| **密钥派生** | HKDF-SHA256 | 直接使用 DH 密钥（不安全）|
| **身份认证** | JWT 双 Token（Access 2h + Refresh 30d）| 单 Token（类型混淆攻击风险）|
| **密码存储** | bcrypt(SHA256(pepper ‖ password)) | MD5/SHA1（不可接受）、单纯 bcrypt（无 pepper）|
| **密钥持久化** | iOS Keychain | UserDefaults（明文存储）|

---

## 三、为何选择当前架构组合

### 3.1 核心决策逻辑

```
需求优先级排序：
安全性 > 实时性 > 离线可用 > 扩展性 > 开发效率

对应架构决策：
安全性  →  E2EE + JWT双Token + Pepper + iOS Data Protection
实时性  →  WebSocket全双工 + Redis Pub/Sub解耦双进程
离线可用 →  本地SQLite全量同步 + 增量since_id拉取
扩展性  →  App/Gateway双进程独立扩展 + MinIO对象存储
开发效率 →  Go标准库 + SwiftUI声明式 + Docker一键启动
```

### 3.2 关键架构决策说明

**① App Server + Gateway 双进程分离**

```
HTTP 请求（低频，需事务）──► App Server ──► PostgreSQL
                                   │
                              Redis Publish
                                   │
WebSocket 长连接（高频）◄── Gateway ◄── Redis Subscribe
```
REST API 写数据库是事务型重操作；WebSocket 推送是高并发轻操作。分离后各自独立扩展，互不影响。

**② E2EE 设计：服务端零知识**

```
Alice 设备                    服务端（不可解密）                Bob 设备
────────                      ──────────────────               ────────
私钥(Keychain)                仅存 Bob.publicKey               私钥(Keychain)
      │                              │                               │
      ├─ ECDH(Alice.priv, Bob.pub) ──┘                               │
      │                                                    ECDH(Bob.priv, Alice.pub)
      └─── 派生相同 AES-256 会话密钥 ◄──────────────────────────────────┘
                │
        AES-GCM 加密消息 ──► 服务端转发（密文）──► AES-GCM 解密
```
服务端只存公钥，永远无法解密消息内容。

**③ 本地 FTS5 全文检索架构**

```
saveMessage()
    │
    ▼
INSERT OR REPLACE INTO local_messages
    │
    ▼  (AFTER INSERT trigger 自动触发)
INSERT INTO messages_fts(rowid, plain_content, ...)
    │
    ▼
searchMessages(query) ──► FTS5 MATCH ──► snippet() 高亮
                      └──► LIKE 兜底（短词/特殊字符）
```

**④ 图片传输优化链路**

```
用户选图 → SHA256 哈希 → HEAD /check 秒传检测
                              │
                    ┌─────────┴─────────┐
                    │ 已存在             │ 不存在
                    │ 直接用 URL         │ 上传原图 + 生成缩略图
                    └──────────┬─────────┘
                               │
                    E2EE 加密 {url, thumbUrl, w, h}
                               │
                    WS 推送 → 接收方先显示缩略图
```

---

## 四、消息收发完整时序图

```
发送方 (Alice)          App Server           Gateway           接收方 (Bob)
────────────            ──────────           ───────           ────────────
│ 输入文字               │                    │                  │
│ E2EE 加密 ────────────►│                    │                  │
│                        │ INSERT messages    │                  │
│                        │ Redis PUBLISH ─────►│                  │
│◄─ ACK (conv_id) ───────│                    │ WS推送密文 ──────►│
│                        │                    │                  │ E2EE 解密
│                        │                    │                  │ saveMessage
│                        │                    │                  │ 本地FTS索引
│                        │                    │                  │ UI更新
```

---

## 五、本地数据层架构图

```
┌─────────────────────────────────────┐
│         DatabaseService (GRDB)      │
│                                     │
│  ┌─────────────────────────────┐   │
│  │     local_messages (主表)    │   │
│  │  id / conv_id / sender_id   │   │
│  │  plain_content / msg_type   │   │
│  │  media_url / thumb_url      │   │
│  │  created_at / is_mine       │   │
│  └──────────────┬──────────────┘   │
│         trigger │ AFTER INSERT/    │
│                 │ UPDATE/DELETE    │
│  ┌──────────────▼──────────────┐   │
│  │    messages_fts (FTS5虚表)   │   │
│  │  tokenize = unicode61        │   │
│  │  content  = local_messages   │   │
│  │  snippet() 高亮检索片段       │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │       db_metadata            │   │
│  │  fts_populated = '1'        │   │
│  └─────────────────────────────┘   │
│                                     │
│  iOS Data Protection                │
│  NSFileProtectionCompleteUnlessOpen │
│  AES-256 (Secure Enclave)           │
└─────────────────────────────────────┘
```

---

## 六、技术栈总览

| 层次 | 技术 | 版本/规格 |
|------|------|---------|
| **iOS UI** | SwiftUI + MVVM | iOS 26.5，Swift 5 |
| **iOS 网络** | URLSession（HTTP + WebSocket）| 原生，无第三方 |
| **iOS 加密** | Apple CryptoKit（X25519 + AES-GCM）| 原生 |
| **iOS 本地 DB** | GRDB 6.29.3（SQLite + FTS5）| SPM 引入 |
| **iOS 安全存储** | iOS Keychain + Data Protection | kSecAttrAccessibleAfterFirstUnlock |
| **后端语言** | Go 1.21+ | 标准库 net/http + gorilla/websocket |
| **关系数据库** | PostgreSQL 15 | Docker，WAL + 连接池 |
| **缓存/消息** | Redis 7 | Docker，Pub/Sub 解耦双进程 |
| **对象存储** | MinIO | Docker，S3 兼容，SHA256 内容寻址 |
| **认证** | JWT（RS256 双 Token）| Access 2h / Refresh 30d |
| **容器编排** | Docker Compose | 一键启动全部基础设施 |

---

## 七、架构优势总结

| 维度 | 实现方式 | 效果 |
|------|---------|------|
| **安全** | E2EE + Pepper + Keychain + Data Protection | 服务端零知识，数据库加密存储 |
| **实时** | WebSocket + Redis Pub/Sub | 消息延迟 < 100ms |
| **离线** | 本地 SQLite 全量同步 + since_id 增量 | 断网可读历史，重连自动补全 |
| **去重** | client_msg_id 部分唯一索引 | 网络重试不产生重复消息 |
| **图片** | SHA256 秒传 + 缩略图先显 | 重复图片零流量，感知速度快 |
| **搜索** | FTS5 content table + 触发器自动维护 | 全量历史全文检索，定位高亮 |
| **单设备互踢** | Redis 记录设备 Token，旧连接强制断开 | 防止僵尸连接，保证消息送达 |
